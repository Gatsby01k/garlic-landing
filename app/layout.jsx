export const metadata = {
  title: "GARLIC — Meme Token Landing",
  description: "Spicy meme token landing page",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ru">
      <body>{children}</body>
    </html>
  );
}
